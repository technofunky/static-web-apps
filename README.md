seq.html does 2 things
creates a plantuml sequence diagram based on user config and inputs (also stored in localstorage)
takes plant uml as input and creates a html table out of it for further updates
